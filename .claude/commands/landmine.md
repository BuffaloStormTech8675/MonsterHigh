Perform a full project audit and find everything that is broken, incomplete, insecure, or risky. Check for:

- Incomplete features or TODOs left in code
- Security vulnerabilities (exposed secrets, unvalidated inputs, SQL injection, XSS, etc.)
- Broken or missing API routes
- Dead code or unused files
- Missing error handling
- Hardcoded credentials or API keys
- Dependencies that are outdated or have known vulnerabilities
- Missing environment variables that would cause crashes
- Inconsistencies between frontend and backend
- Any features that are partially implemented but not wired up

Report findings grouped by severity: Critical, High, Medium, Low. Be specific — include file names and line numbers where possible.
